#!/usr/bin/env sh

echo "=== Running Local CI Pipeline ==="

# Format check
npx prettier --check "src/**/*.{ts,tsx}" || exit 1

# ESLint
npx eslint "src/**/*.{ts,tsx}" || exit 1

# Unit tests
npm run test:ci || exit 1